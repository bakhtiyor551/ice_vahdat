# Для сборки под телефон: укажите IPv4 компьютера с backend (тот же Wi‑Fi).
# Узнать IP в Windows: Win+R → cmd → ipconfig → «IPv4-адрес».
# Порт — как в backend (по умолчанию 3847).
VITE_API_URL=http://192.168.0.10:3847
